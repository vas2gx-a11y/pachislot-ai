# pachislot-ai

パチスロの実戦記録を貯めて、設定推測・店舗の傾向分析をするFlaskアプリ。
データの実体はGoogleスプレッドシート（DBは持たない）。Renderにデプロイ。

## 構成

- `app.py` — Flask本体。Blueprint登録、gzip圧縮、ナビの現在地判定のみ。
- `common.py` — **5,200行。ロジックは全部ここ**（下の地図を参照）
- `routes/*.py` — 画面ごとのBlueprint。薄い。`common.py` の関数を呼ぶだけ。
- `navigation.py` — サイドバー/モバイルナビの構成定義。
- `templates/` — Jinja2。`base.html` と `_macros.html` が共通土台。
- `static/js/` — 設定判別のブラウザ側コード。他の画面はJSをテンプレートに直書きしているが、
  判別だけは分量と「計算と表示を混ぜない」方針からファイルに切り出している。
- `judge_db.py` — 設定判別の機種スペック（SQLite）。**唯一シートを使わない層**。理由はファイル冒頭。
- `machine_info.py` / `machine_data/*.json` — 機種情報（解析まとめ）。JSONが正で、`/info` はそれを描くだけ。
  判別DBへはボタンで明示的に取り込む（自動同期しない理由はファイル冒頭）。書き方は `machine_data/README.md`。
- `tools/` — アナスロ(ana-slo.com)からホールデータを取り込むCLI。使い方は `tools/README.md`。
- `data/` — 取り込みの中間ファイル置き場。コミット対象外の作業データ。判別DBもここ（`judge.db`）。

## 設定判別（ベイズ推定）

`/judge` 系。他の機能と違い、データの実体はシートではなくSQLiteとブラウザのlocalStorage。

| 置き場所 | 中身 | なぜそこか |
|---|---|---|
| SQLite（`judge_db.py`） | 機種ごとの判別スペック | 「1機種 : 判別要素 : 選択肢」の入れ子をシートで持てない |
| localStorage（`static/js/judge.js`） | 店舗条件・判別ログ | その端末で打つ人の持ち物。共有する意味がない |

- `static/js/bayes.js` — 計算だけ。**DOMには触らない**。色やHTMLもここには置かない
  （数値の正しさが結果に直結するため、表示の都合で書き換えられないよう隔離している）。
- `static/js/judge.js` — 判別画面の入出力。`bayes.js` を呼ぶだけ。
- `static/js/judge_form.js` — スペック登録フォームの行の増減。判別力の目安表示は `bayes.js` を再利用する
  （登録画面と判別画面で別実装にすると、片方だけ直したときに表示がずれるため）。
- `routes/judge.py` — 画面とスペックのCRUD、`/judge/api/machines`。**推定はサーバーでは行わない**
  （入力するたびに事後確率が動くのが使い方なので、1回ごとに往復させると打ちながら使えない）。

- 設定1が無い機種などは `judge_machines.available_settings`（"011111" 形式）で非搭載設定を持つ。
  判別画面では確定演出と同じ仕組みで非搭載設定を常に候補から外す（`judge.js` の `getConfirmations`）。
  DBの6列は NOT NULL なので、非搭載設定の列には搭載中の最低設定の値を入れている（事後確率0なので計算には効かない）。

判別スペックは**サーバーが正**。判別ページはレンダリング時にJSONを埋め込んで渡すので、
ブラウザ側にスペックのコピーは持たせない（どちらが正か曖昧になるのを避けるため）。

天井・期待値のスペックはシート側（`machines`）の管轄で、判別DBには持たせていない。
同じ項目を2か所に置かないため。
機種情報JSONにも天井を書くが、出典と照らし合わせるための表示用で、期待値計算には流し込まない。

## common.py の地図

**全体を読まないこと。**該当セクションだけ `sed -n 'A,Bp' common.py` で開く。

| 行 | 内容 |
|---|---|
| 23-247 | インメモリキャッシュ、集計キャッシュ、各種設定定数 |
| 248-1198 | スプレッドシート接続、各シートのload/save |
| 1199-1345 | 画像解析（Gemini） |
| 1346-1513 | URLから機種スペックを取り込む |
| 1514-2026 | 設定推測ロジック |
| 2027-2159 | 分析結果へのQ&A（セッション単位のチャット） |
| 2160-2258 | 期待値計算（天井/ゾーン狙い） |
| 2259-3001 | 店舗の傾向分析（自分の実戦記録から集計） |
| 3002-3217 | 店舗の年間データ（外部集計の登録・読み込み） |
| 3218-3493 | ホールデータの貼り付け取り込み |
| 3494-3893 | 旧イベント日・周年日の解釈 |
| 3894-4329 | 台別データ（機種・台番号ごと）の貼り付け取り込み |
| 4330-4817 | 取り込みログ（取り込みの取り消し） |
| 4818-5222 | イベントカレンダー（月別） |

## 決まりごと

- コメントは日本語。**なぜそうしたか**を書く（何をしているかはコードを読めば分かる）。
  既存のコメントの密度と口調に合わせる。
- セクション区切りは `# ---...---` + 見出し + 意図の説明、の形を踏襲する。
  `common.py` にセクションを増やしたら、上の地図も直す。
- シートを読む処理は必ずキャッシュ層（`_cache_get` / `_cached_by_data_version`）を通す。
  書き込んだら対応するキャッシュを `_cache_invalidate` する。
- 保存系の関数は「同じキーなら上書き」で書く（取り込みのやり直しが効くように）。

## 環境変数

`SPREADSHEET_ID` / `GOOGLE_SERVICE_ACCOUNT_JSON` / `GEMINI_API_KEY` が必須。
シート名は `SHEET_NAME`、`MACHINES_SHEET_NAME` など個別に上書き可（既定値は `common.py` 冒頭）。
`JUDGE_DB_PATH` は判別スペックのSQLiteの置き場所（既定 `data/judge.db`）。

## 注意

- ana-slo.com はCloudflareでbotを弾くので、`requests` / `curl` では取れない。
  取得はブラウザ側、解析はPython側という分担になっている（`tools/README.md`）。
- `?refresh=1` を付けるとキャッシュを捨ててシートから読み直す。
- Renderの無料プランはディスクが揮発性なので、再デプロイのたびに `data/judge.db` は消える。
  判別スペックを残すには永続ディスクを用意して `JUDGE_DB_PATH` をそこに向ける。
  localStorage側（店舗条件・判別ログ）はブラウザにあるので、サーバー側が消えても影響しない。
